# ------------------------------------------------------
# THIS FILE WAS AUTOMATICALLY GENERATED (DO NOT MODIFY)
# ------------------------------------------------------

type Author {
  country: String
  id: ID!
  name: String!
}

type Book {
  author: Author!
  genre: Genre!
  id: ID!
  publishedYear: Int
  title: String!
}

type BookConnection {
  edges: [BookEdge!]!
  pageInfo: PageInfo!
  totalCount: Int!
}

type BookEdge {
  cursor: String!
  node: Book!
}

input BookFilterInput {
  genre: Genre
  publishedYearFrom: Float
  publishedYearTo: Float
}

type BookNotFoundError {
  bookId: String!
  message: String!
}

union BookResult = Book | BookNotFoundError | BookValidationError

type BookValidationError {
  field: String!
  message: String!
}

input CreateBookInput {
  authorId: String!
  genre: Genre!
  publishedYear: Int
  title: String!
}

enum Genre {
  FICTION
  HISTORY
  NON_FICTION
  SCIENCE
  TECHNOLOGY
}

type Mutation {
  createBook(input: CreateBookInput!): Book!
  deleteBook(id: ID!): Boolean!
  updateBook(id: ID!, input: UpdateBookInput!): Book
}

type PageInfo {
  endCursor: String
  hasNextPage: Boolean!
  hasPreviousPage: Boolean!
  startCursor: String
}

input PaginationInput {
  after: String
  first: Float! = 10
}

type Query {
  book(id: ID!): Book
  bookConnection(filter: BookFilterInput, pagination: PaginationInput!): BookConnection!
  bookOrThrow(id: ID!): Book!
  bookResult(id: ID!): BookResult!
  books: [Book!]!
  booksByIds(ids: [ID!]!): [BookResult!]!
}

input UpdateBookInput {
  authorId: String
  genre: Genre
  publishedYear: Int
  title: String
}