import { InputType, Field, Int } from '@nestjs/graphql';
import { Genre } from '../books.model';

// TODO: @InputType() と @Field() を使って CreateBookInput を定義してください
// フィールド: title(必須), author(必須), publishedYear(任意), genre(必須)
